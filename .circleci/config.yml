version: 2.1
orbs: 
  aws-auth: cardig/aws-auth@1.3.0
executors:
  python_executor:
    docker:
      - image: cimg/python:3.9.13
jobs:
  SME_code_review: 
    executor: python_executor
    steps:
      - checkout
      - run: echo "SME COde Review"
      - run:
          name: SME_Script
          command: |
            python sme_script.py ahteshamahmad
workflows:
  bt_workflow:
    jobs:
      - SME_code_review
